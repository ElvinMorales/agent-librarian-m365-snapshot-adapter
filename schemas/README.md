# Vendored source manifest schema

`source-manifest.schema.json` is an unmodified copy of schema version `0.1.0`
from `agent-librarian/src/agent_librarian/schema_data/source-manifest.schema.json`,
reviewed on 2026-06-29. Tests validate every generated manifest against this
copy. Compatibility changes must update this note, the schema copy, and tests.
